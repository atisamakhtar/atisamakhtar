/**
 * MetaTags — supplemental tags not covered by Next.js Metadata API.
 * Primary SEO is handled via generateMetadata / createPageMetadata.
 */
export function MetaTags() {
  return (
    <>
      <meta name="author" content="Atisam Akhtar" />
      <meta name="theme-color" content="#0a0a0b" />
    </>
  );
}
